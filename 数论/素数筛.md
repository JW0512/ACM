```cpp
void GetPrime()
{
	int m=sqrt(maxn+0.5);
	memset(isPrime,false,sizeof(isPrime));
	isPrime[2]=true,prime[cnt++]=2;
	for(int i=3;i<=maxn;i+=2)//初始化奇数为素数 
		isPrime[i]=true;
	for(int i=3;i<=maxn;i+=2){ //筛除奇数中的合数 
		if(isPrime[i]){
			for(int j=i*3;j<=maxn;j+=i*2)	//筛除i的3、5、7...倍 
				isPrime[j]=false;
		} 
	}
	for(int i=3;i<=maxn;i+=2)
	{
		if(isPrime[i])
		{
			prime[cnt++]=i;
		} 
	}
 } 
```